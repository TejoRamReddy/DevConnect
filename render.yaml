services:
  - type: web
    name: devconnect
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: devconnect_db
          property: connectionString

databases:
  - name: devconnect_db
    plan: free